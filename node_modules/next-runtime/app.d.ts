import { AppProps } from 'next/app';
import { ReactElement } from 'react';
export declare function withNextRuntime(BaseApp?: (props: AppProps) => ReactElement): ({ pageProps, ...rest }: AppProps) => JSX.Element;
