export declare class FetchError extends Error {
    status: number;
    statusText: string;
    headers: Headers;
    data?: Record<string, unknown>;
    constructor(response: Response, data: Record<string, unknown>);
    static create(response: Response): Promise<FetchError>;
}
