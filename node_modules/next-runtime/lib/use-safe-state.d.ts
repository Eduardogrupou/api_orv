/**
 * Sets state while avoiding the "set state while unmounted" warning,
 * can eventually be removed in react 18 (hopefully!)
 */
export declare function useSafeState<T>(initial: T): readonly [T, (value: T) => void];
